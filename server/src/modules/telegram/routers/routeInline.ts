import type { PipelineContext } from '../types.js';

export const routeInline = async (_ctx: PipelineContext) => {
  // Stub: inline queries are supported by the pipeline but not handled yet.
  return false;
};
