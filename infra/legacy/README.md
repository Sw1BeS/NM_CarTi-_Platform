# Legacy Infra Artifacts

- `infra/legacy/api-stub/*` — historical Express stub (501 for `/api/*`).
- Not used in production deploy path (`infra/deploy_prod.sh` + `infra/docker-compose.cartie2.prod.yml`).
- Kept only for forensic/debug reference.
